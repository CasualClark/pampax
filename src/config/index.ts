export { featureFlags, type FeatureFlags } from './feature-flags.js';
export { logger, type LoggerConfig, type LogEntry, type LogLevel } from './logger.js';

// Re-export policy configuration types
export type { PolicyConfig } from '../policy/policy-gate.js';