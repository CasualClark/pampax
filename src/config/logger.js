/**
 * Logger for tokenizer factory - now uses structured logging
 */

import { getLogger } from '../utils/structured-logger.js';

export const logger = getLogger('tokenizer-factory');

export default logger;